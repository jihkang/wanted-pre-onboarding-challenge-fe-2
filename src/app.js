
/**
 * todo list 생성.
 * @constructor
 * @param {string} id - required
 * @param {string} context - required
 */
function createList(id, context) {}

/**
 * @param {string} type 대상을 찾는것인지, 전체를 출력하는지 결정.  
 * @param {string} id   type이 대상을 찾는것이라면 해당 리스트의 아이디. 
 */
function readList(type, id) {}

/**
 * id 를 제외한 모든 값을 수정 가능함.
 * @param {string} list 수정할 대상.
 * @param {string} type 대상이 태그인지, 속성인지 
 * @param {object} item 수정한 결과값 을 가지고 있어야함. 
 */
function updateList(list, type, item) {}
/**
 * @param {string} id 딜리트 해야하는 대상.
 */
function deleteList(id) {

}